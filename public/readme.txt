This will call a new application (instance), basically an object that will be /app/core/
Then it will read the url if available and call the 'right' controller, which will render the right 'view'.


//the reason why the files are in a folder called public is because i want you guys too point the server root to the
public folder on the server. This makes it so the stuff before it cannot be accessed by users, just scripts.
